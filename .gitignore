# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Environments
.venv/
venv/
.env
.env.*
!.env.example

# App local state (persisted Powens token / id_user) — never commit
.powens_state.json
powens_state.json

# Private category rules (real merchant names) — never commit
categories.local.json

# Local store: balance history, category overrides, series state — never commit
.powens_finance.db
*.db-journal
*.db-wal
*.db-shm
.backups/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editors
.DS_Store
.idea/
.vscode/
.gstack/
